# Archive

Closed ComDevKB projects land here, moved as a full directory (not a
subset) once a project reaches a real "done" state — see `AGENTS.md`'s
Concepts section for what counts as a project vs. an effort. Not deleted:
a closed project is still a valid historical record. Nothing has closed
here yet.
